using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Layers : MonoBehaviour
{
    public static LayerMask OUT = LayerMask.GetMask("Out");
    public static LayerMask KEEP = LayerMask.GetMask("Keep");
    public static LayerMask FALL = LayerMask.GetMask("Fall");
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
